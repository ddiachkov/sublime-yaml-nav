[
  { "keys": [ "ctrl+r" ], "command": "goto_yaml_symbol", "context":
      [
        { "key": "selector", "operator": "equal", "operand": "source.yaml" }
      ]
  },

  { "keys": [ "ctrl+shift+w" ], "command": "copy_yaml_symbol_to_clipboard", "context":
      [ 
        { "key": "selector", "operator": "equal", "operand": "source.yaml" }
      ]
  }
]